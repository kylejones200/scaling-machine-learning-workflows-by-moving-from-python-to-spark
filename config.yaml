data:
  source: null
  generate_synthetic: true
  n_samples: 10000
  n_features: 10
  seed: 42

spark:
  app_name: "MLPipeline"
  use_spark: false

output:
  figures_dir: "images"
  figure_format: "png"
  figure_dpi: 100

